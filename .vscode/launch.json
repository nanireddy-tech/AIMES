{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "MedLens API",
      "type": "debugpy",
      "request": "launch",
      "module": "uvicorn",
      "args": ["backend.app.main:app", "--reload", "--port", "8000"],
      "cwd": "${workspaceFolder}/medlens",
      "envFile": "${workspaceFolder}/medlens/.env.example"
    }
  ]
}
